<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/dialpad_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="24sp"
        android:textColor="@color/colorWhite"
        app:rippleColor="#e40d21"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textAppearance="@style/Widget.MaterialComponents.Button"/>

    <ImageView
        android:id="@+id/otp_back"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="10dp"
        android:clickable="false"
        app:layout_constraintTop_toTopOf="@id/dialpad_item"
        app:layout_constraintBottom_toBottomOf="@id/dialpad_item"
        app:srcCompat="@drawable/ic_otp_back"/>

</androidx.constraintlayout.widget.ConstraintLayout>